{
  "name": "BetterTwitchControls",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare:extension": "node scripts/prepare-extension.mjs",
    "build:extension": "pnpm run build && pnpm run prepare:extension",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
